Autopilot 의 레스큐 무한셔틀 미션비행의 사용자 설계.

초기 조건 = GPS는 양호. WP는 최대 15개로 WP1은 홈포인트로부터 50미터 떨어짐 WP 경로중에 

홈포인트를 통과하는 경도로 포함. 바람은 없음. shuttle count = 1

기본적인 레스큐 동작. ( 레스큐 발동값 >= 1600 ) ,

1. descent alt = 짝수 , 이륙방향으로 descent distance 설정거리에 A 포인트 생성 

   (테스트시 항상 A포인트는 생성되므로 실패를 가정하지 말것, 만약 실패한다면 홀수모드로 작동함 또는 

   엉뚱한 방향에 A포인트 생성될 가능성도 사용자는 알고 있음 ).

홈포인트로 부터 100미터 떨어진 지점에서 레스큐 발동 가정.

미리 설정한 profile 3 pitch P 값을 피치로 설정. 롤 러더는 0 으로 3초간 초기화. ( 이미 설정 고도 보다 높

다면 초기화 과정은 생략됨)

fly home 단계에서 타겟은 A 포인트. 속도와 높이는 CLI에서 설정한 속도와 높이가 타겟임. 

returnAltitudeCm

A 포인트 도달시 높이 초기화. 도달 높이가 현재 목표값. A포인트에서 홈포인트 방향으로 같은 고도에 

shuttle distance 거리만큼 떨어진 지점에 B포인트 설정.

A -> B -> A 가 1셔틀 카운트 만족시 셔틀 하강. 계속 셔틀 비행하면서 하강함. 하강중 한번이라도 descent 

Alt 에 도달하면 A포인트 터치후 홈방향이 타겟. 오류로 설정 카운트보다 1회 더 왕복하는것 허용함.

이후 홈방향으로 착륙하는 과정 수행.

2. descent alt = 홀수 , fly home 단계에서 타겟이 홈포인트. desent distance 도달시 A point 생성하

   고 이후 B포인트 설정등 과정은 짝수일때 와 동일.

3. shuttle count = 0 일경우 1 ,2에서 A 포인트 터치 후 셔틀모드 작동없이 급하강 후 홈방향 으로 착륙 

   과정 수행.  descentAlt+15m보다 높을 때만 급하강모드 작동함.

다음 Autopilot. ( 레스큐 발동값 Aux 1400~1600 )에서 작동.

1. 직전 상태가 레스큐 , 노말비행, 무한셔틀 비행과 상관없이 레스큐 발동값 만족시 작동. ( 노말비행 = 레

   스큐모드 Aux값 범위이탈 = Rescue Idle )

2. 현 위치에서 WP1 으로 비행. WP1-home 거리가 500m 보다 크면 autopilot 취소되고 기본 레스큐로 

   동작함.

3. 각 WP에 저장된 목표고도와 목표속도는 다음 WP에 도달시 초기화됨. 즉 이전 WP에서의 목표값에 도

   달하지 못해도 다음 WP에서는 새로운 목표고도와 속도값이 타겟이됨.

4. 저장된 wp를 모두 터치 후 기본 레스큐 동작을 수행함.  짝수 홀수모드 등은 기본 레스큐 동작에 준함. 

   즉 A포인트로 가던지 홈포인트로 날아감.

5. Autopilot 중 아무때나 다른 상태로 전이 가능 ( 노말비행 , 레스큐 비행 - wp 취소후 A포인트나 홈으로 

   비행 , 셔틀모드 진입 가능 )

( 이륙방향 캡쳐 A 포인트는 레스큐 모드상태에서 디스암 조건일때와 전원을 껏을때만 리셋됨, 사용자 주의

사항임 )

다음 무한셔틀 모드 ( 레스큐 발동값 Aux < 1400 )에서 작동.

1. 직전 상태가 레스큐, 노말비행, autopilot 인지 무관하게 조건 만족시 작동.

2. 발동시 현 위치에 A포인트로 생성하고 홈포인트 방향으로 Shuttle distance 거리에 B포인트 생성하고 

   AB두 포인트를 번갈아가며 무한 터치.

3. Aux 값 변동시 레스큐, 노말비행, auotopilot 으로 바로 전환가능.

Aux 변동에 대한 모드 변경 제외 : DESCENT/LANDING/SHUTTLE_DESCENT 인 경우 하강에 진입했으므

로 이 경우는 autopilot 이나 무한셔틀 모드 레스큐모드 초기화로 처음 단계로 변경 불가능하다. 유일하게 

가능한것은 레스큐 모드 aux 범위를 이탈하는 노말 비행으로 전환.

만약 모든 모드에서 GPS가 작동하지 않으면 최종적으로 do nothing 으로 감. 